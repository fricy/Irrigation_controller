# ============================================================================
# zones/zigbee_zone_endpoints_11.yaml  —  11-zone ZB endpoint definitions
# ============================================================================
# Zone endpoints EP 1-11 using cluster 0xFFF0.
# Global endpoints (EP 12/13/14) are in zigbee_global_endpoints.yaml.
# ============================================================================

zigbee:
  id: zb

  endpoints:
    # -------------------------------------------------------------------------
    # Zone 1
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 1
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone1_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 1;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 1;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 1;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z1_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[0].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z1_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[0].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z1_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[0].manual_duration_min = (uint8_t)x; config_save();'
            - id: z1_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[0].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 2
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 2
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone2_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 2;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 2;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 2;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z2_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[1].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z2_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[1].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z2_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[1].manual_duration_min = (uint8_t)x; config_save();'
            - id: z2_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[1].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 3
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 3
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone3_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 3;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 3;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 3;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z3_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[2].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z3_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[2].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z3_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[2].manual_duration_min = (uint8_t)x; config_save();'
            - id: z3_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[2].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 4
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 4
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone4_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 4;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 4;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 4;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z4_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[3].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z4_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[3].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z4_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[3].manual_duration_min = (uint8_t)x; config_save();'
            - id: z4_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[3].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 5
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 5
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone5_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 5;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 5;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 5;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z5_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[4].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z5_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[4].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z5_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[4].manual_duration_min = (uint8_t)x; config_save();'
            - id: z5_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[4].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 6
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 6
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone6_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 6;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 6;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 6;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z6_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[5].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z6_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[5].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z6_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[5].manual_duration_min = (uint8_t)x; config_save();'
            - id: z6_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[5].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 7
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 7
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone7_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 7;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 7;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 7;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z7_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[6].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z7_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[6].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z7_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[6].manual_duration_min = (uint8_t)x; config_save();'
            - id: z7_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[6].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 8
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 8
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone8_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 8;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 8;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 8;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z8_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[7].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z8_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[7].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z8_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[7].manual_duration_min = (uint8_t)x; config_save();'
            - id: z8_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[7].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 9
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 9
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone9_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 9;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 9;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 9;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z9_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[8].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z9_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[8].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z9_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[8].manual_duration_min = (uint8_t)x; config_save();'
            - id: z9_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[8].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 10
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 10
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone10_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 10;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 10;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 10;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z10_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[9].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z10_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[9].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z10_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[9].manual_duration_min = (uint8_t)x; config_save();'
            - id: z10_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[9].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'

    # -------------------------------------------------------------------------
    # Zone 11
    # -------------------------------------------------------------------------
    - device_type: ON_OFF_OUTPUT
      num: 11
      clusters:
        - id: ON_OFF
          attributes:
            - attribute_id: 0
              type: bool
              report: true
              device: zone11_zb
              on_value:
                then:
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0;'
                            then:
                              - lambda: 'id(zb_target_zone) = 11;'
                              - script.execute: jump_to_zone_in_cycle
                            else:
                              - lambda: 'id(manual_zone_selected) = 11;'
                              - script.execute: start_single_zone_manual
                  - if:
                      condition:
                        lambda: 'return !id(zb_internal_sync) && !x;'
                      then:
                        - if:
                            condition:
                              lambda: 'return id(cycle_running) && id(current_cycle_num) != 0 && id(current_zone_num) == 11;'
                            then:
                              - script.execute: skip_zone_script
                            else:
                              - script.execute: stop_single_zone_manual
        - id: 0xFFF0
          attributes:
            - id: z11_irr
              attribute_id: 0x0001
              type: U8
              access: READ_WRITE
              value: 15
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[10].irr_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z11_short
              attribute_id: 0x0002
              type: U8
              access: READ_WRITE
              value: 10
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[10].short_duration_min = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
            - id: z11_manual
              attribute_id: 0x0003
              type: U8
              access: READ_WRITE
              value: 5
              report: true
              on_value:
                then:
                  - lambda: 'if (x > 60) return; g_config.zones[10].manual_duration_min = (uint8_t)x; config_save();'
            - id: z11_flags
              attribute_id: 0x0004
              type: U8
              access: READ_WRITE
              value: 3
              report: true
              on_value:
                then:
                  - lambda: 'g_config.zones[10].flags = (uint8_t)x; config_save(); id(update_next_schedule_display).execute();'
